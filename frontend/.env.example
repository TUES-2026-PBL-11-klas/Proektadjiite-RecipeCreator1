# Frontend-specific environment configuration

# During local development (vite dev server), point directly at the backend.
# For example, if Flask runs on http://localhost:5001:
VITE_API_BASE_URL=http://localhost:5001

